# DeepSeek's official API supports OpenAI-compatible chat completions.
# Add your key here or set DEEPSEEK_API_KEY in the shell before starting.
AI_PROVIDER=deepseek
AI_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=
AI_CHAT_MODEL=deepseek-v4-flash
# Structured graph extraction is more predictable without extended reasoning.
AI_THINKING_MODE=disabled

# DeepSeek does not document an embeddings endpoint. Local vectors keep imports
# and search available without sending additional text to another provider.
AI_EMBEDDING_PROVIDER=local
# To use a dedicated OpenAI-compatible embedding service instead:
# AI_EMBEDDING_PROVIDER=api
# AI_EMBEDDING_BASE_URL=https://your-embedding-provider.example/v1
# AI_EMBEDDING_API_KEY=
# AI_EMBEDDING_MODEL=

# Scanned-page OCR is independent from DeepSeek graph extraction.
# Keep OCR_PROVIDER=local to use Tesseract without cloud uploads, or set aliyun
# and provide a RAM user's OCR-authorized credentials below.
OCR_PROVIDER=aliyun


HOST=127.0.0.1
PORT=4310
DATA_DIR=./data

# GraphRAG v6 rollout switches. Defaults use the v2 index profile.
INDEX_PROFILE=v2
ENABLE_CONTEXT_UNITS=true
ENABLE_V2_PULSE_PACK=true
ENABLE_CONTEXT_UNIT_GRAPH_EXTRACTION=true
SHOW_DEBUG_RETRIEVAL=true
DEBUG_API_ALLOW_UNAUTH_LOCAL=true
DEBUG_MAX_TEXT_LENGTH=40000
RUN_REAL_MODEL_TESTS=true

# AORI answering path. traversal is the default: AORI map -> BFS/DFS -> chunks.
# Optional: legacy, strict_evidence_table
AORI_ANSWER_MODE=demand

# Model context profile. Prefer explicit values for long-context providers;
# otherwise the server uses a conservative compact retrieval profile.
MODEL_CONTEXT_STRATEGY=retrieval-compact
MODEL_MAX_INPUT_TOKENS=8192
MODEL_PREFERRED_CONTEXT_TOKENS=6000
COMPACT_EXCERPT_TOKENS=12000

# Production access control. When AUTH_REQUIRED=true, users must register or
# log in before using the app. Registration also requires one of these keys.
# Keep REGISTRATION_KEYS private and rotate/remove keys whenever needed.
# AUTH_REQUIRED=true
# REGISTRATION_KEYS=replace-with-your-invite-key
# AUTH_SESSION_DAYS=30
# Set true only when serving the app over HTTPS.
# AUTH_COOKIE_SECURE=false
